plugins {
    id 'java-library'
    id 'com.gradlehero.themepark-conventions'
}

tasks.named('jar') {
    manifest {
        attributes('Main-Class': 'com.gradlehero.thempark.RideStatusService')
    }
}